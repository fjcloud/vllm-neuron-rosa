apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: vllm-neuron
  labels:
    app: vllm-neuron
  annotations:
    haproxy.router.openshift.io/timeout: "300s"
spec:
  to:
    kind: Service
    name: vllm-neuron
  port:
    targetPort: 8000
  tls:
    termination: edge
